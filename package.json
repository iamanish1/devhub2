{
  "name": "devhubs-fullstack",
  "version": "1.0.0",
  "description": "DevHubs Full Stack Application",
  "private": true,
  "scripts": {
    "build": "cd Server && npm ci --only=production",
    "start": "cd Server && npm start",
    "dev": "cd Server && npm run dev"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "workspaces": [
    "Server",
    "client"
  ]
}
